{{#each artists}}

    <div class="column is-4">
        {{> favorites/artists/artist}}
    </div>

{{/each}}

<div class="column is-4">
    <div id="modal-btn" class="has-text-centered">
        <span class="add-sign">(integrate with favorites button on search results)</span>
    </div>
</div>
